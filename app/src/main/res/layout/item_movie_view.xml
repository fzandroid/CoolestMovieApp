<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/ParentLayout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="10dip">

    <TextView
        android:id="@+id/MovieNameLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_marginBottom="10dip"
        android:layout_toLeftOf="@+id/LikeImg"
        android:textSize="15dp"
        android:textStyle="bold"
        tools:text="Movie Name :Titanic (1999)" />

    <ImageView
        android:id="@+id/LikeImg"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignParentRight="true"
        android:src="@drawable/ic_heart_unliked" />

    <ImageView
        android:id="@+id/PosterImg"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/MovieNameLabel"
        android:layout_marginBottom="10dip"
        android:adjustViewBounds="true"
        android:scaleType="fitCenter"
        android:src="@drawable/ic_small_search"
        android:transitionName="@string/transition_image" />

    <TextView
        android:id="@+id/PlotLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/MovieNameLabel"
        android:layout_marginLeft="10dip"
        android:layout_toRightOf="@+id/PosterImg"
        tools:text="GitHub is home to over 31 million developers working together to host and review code, manage projects, and build software together." />

    <TextView
        android:id="@+id/DirectorNameLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/PlotLabel"
        android:layout_marginLeft="10dip"
        android:layout_marginTop="10dp"
        android:layout_toRightOf="@+id/PosterImg"
        android:textStyle="italic"
        tools:text="Director by Christopher Nohlhan" />
</RelativeLayout>